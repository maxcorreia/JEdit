package cs3500.image.command;

/**
 * This is an interface to group the commands that the user has access to.
 * Since they all have different arguments in the first part of this assignment,
 * it seems reasonable to use this interface for the sole purpose of grouping
 * these commands together with the marker interface pattern.
 */
public interface Command {
}
